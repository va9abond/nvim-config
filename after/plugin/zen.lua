-- ---------------------------------------------------------
                   -- https://github.com/folke/zen-mode.nvim
-- ---------------------------------------------------------
require('zen-mode').setup({
    plugins = {
        gitsigns = { enable = true },
        tmux = { enable = false }
    }
})
